FREEZE — Gauntlet run 2 (opened 2026-08-31)
IN: idle input screen (cold and armed), report screen, tab bar
(all states), export document. Ivory Loom only. Run Audit CTA
armed-state text → text-navy (Commander ruling 2026-08-31);
cold-state disabled pill unchanged (WCAG 1.4.3 exempts inactive
controls — quote the clause in the report when claiming it).
OUT: engine (lib/, app/api/), prompts (9 parked vocab hits —
separate key), retry-on-transport (2.1), IA_CANON strings beyond
banned-synonym fixes, any new feature, any new hue/hex.
DEFECT: any item failing the gauntlet checklist. Fixing broken is
always legal.
THAW: run-2 gauntlet report certified by the Tower.
PARKED: surface-list consolidation into lib.mjs; prompts vocab key.
