# gauntlet/run-1-final

The 24 shots as they stand at the end of Gauntlet run 1, committed so
`GAUNTLET_REPORT.md`'s before/after grid resolves on the branch.

"Before" is `gauntlet/baseline/` — the committed cycle-0 original, which
run 1 never overwrote. "After" is this folder.

These are a report artifact, not instrument output. `gauntlet/out/` stays
gitignored and regenerated; nothing reads this folder.
